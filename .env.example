# ShopSphere Python Marketplace Environment Configuration
# NEVER COMMIT REAL PRODUCTION CREDENTIALS TO SOURCE CONTROL

# Django Server Settings
DJANGO_SECRET_KEY=synthetic_shopsphere_django_secret_key_2026_prod_secure_998877
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration (Defaults to SQLite for portable dev, PostgreSQL supported)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=data/shopsphere_django.sqlite3
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=shopsphere_db
# DATABASE_USER=shopsphere_user
# DATABASE_PASSWORD=shopsphere_password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Application Settings
SITE_NAME=ShopSphere Marketplace
COMMISSION_DEFAULT_RATE=8.50
CURRENCY_CODE=USD
STATIC_URL=/static/
MEDIA_URL=/media/
